feat: prevent adding the same Dimplex account twice

Set the config-entry unique id to the stable Dimplex account id (UserContext.Id)
and abort the flow with already_configured when that account is already set up.

- api.py: capture and expose the account id via a new account_id property,
  populated when async_validate_connection / async_exchange_code fetch the user
  context (no extra API call — the context was already fetched and discarded).
- config_flow.py: validate_credentials / validate_auth_code now return
  (token_data, account_id); the credentials and auth-code add steps call
  async_set_unique_id + _abort_if_unique_id_configured. Reauth steps unpack the
  tuple unchanged.
- translations: add the already_configured abort string to en/fr/nb (parity
  check passes).

Tests: config-flow success/reauth mocks updated to the tuple shape, new
duplicate-account abort test, and api tests assert account_id capture. Full
suite green at 85.32%.
