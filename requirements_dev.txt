# Home Assistant, pinned to the version CI tests against.
#
# These two move together: pytest-homeassistant-custom-component pins an exact
# `homeassistant==` build, so upgrading either alone leaves an unsatisfiable
# resolution and a red CI. An unpinned `homeassistant` was not "testing latest
# HA" either — it was whatever the PHACC floor happened to drag in, which changes
# silently every time PHACC is bumped (dimplex-controller-hass#201).
#
# Dependabot's `python-minor-patch` group updates both in a single PR, and its pip
# entry covers this file. The declared HACS floor (hacs.json) is the *minimum*
# supported; it is deliberately not the tested version.
homeassistant==2026.9.3
pytest-homeassistant-custom-component==0.13.366

dimplex-controller>=0.13.1

# Single source of truth for the docs generator; see the file for why it is pinned.
-r requirements_docs.txt
